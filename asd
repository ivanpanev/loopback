{
  "reason": "conflicting action statements: uri, set_fact\n\nThe error appears to be in '/runner/project/roles/netbox_lookup/tasks/lookup_one_gnid.yml': line 12, column 3, but may\nbe elsewhere in the file depending on the exact syntax problem.\n\nThe offending line appears to be:\n\n# 1. Fire off async NetBox queries (one per role)\n- name: Query NetBox for GNID {{ gnid }} in role {{ role_id }}\n  ^ here\nWe could be wrong, but this one looks like it might be an issue with\nmissing quotes. Always quote template expression brackets when they\nstart a value. For instance:\n\n    with_items:\n      - {{ foo }}\n\nShould be written as:\n\n    with_items:\n      - \"{{ foo }}\"\n"
}
